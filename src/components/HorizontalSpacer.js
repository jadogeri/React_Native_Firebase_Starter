import {  View } from 'react-native'
import React from 'react'

const HorizontalSpacer = ({marginTop, marginBottom}) => {
  return (
    <View style={{marginTop : marginTop, marginBottom : marginBottom}}>
    </View>
  )
}

export default HorizontalSpacer

